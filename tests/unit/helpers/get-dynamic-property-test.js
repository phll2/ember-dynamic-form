import { getDynamicProperty } from 'dummy/helpers/get-dynamic-property';
import { module, test } from 'qunit';

module('Unit | Helper | get dynamic property');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getDynamicProperty([42]);
  assert.ok(result);
});
